import { CurrentformatPipe } from './currentformat.pipe';

describe('CurrentformatPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentformatPipe();
    expect(pipe).toBeTruthy();
  });
});
